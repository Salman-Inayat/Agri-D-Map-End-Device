import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  tabPanel: {
    paddingRight: theme.spacing(2),
  },
  resultImage: {
    height: "300px",
    width: "300px",
    marginTop: "30px",
  },
}));
